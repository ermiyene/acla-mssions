/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "aclachurch.org",
      "equipemedia.com",
    ],
  },
};

export default nextConfig;
